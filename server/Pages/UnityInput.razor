@page "/{text}"
@using server.Data
@inject UnityService ShipService

<h1>@WebText</h1>

@code {
    [Parameter]
    public string Text { get; set; }
    public string WebText = "";
    int shipId;

    protected override void OnInitialized()
    {
        Text = Text ?? "none";

        ShipService.SendMessage(Text);
        WebText = "Unity was sent the command \"" + Text + "\".";
    }
}